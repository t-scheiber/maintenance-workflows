{
  "name": "personal-public-maintenance-workflows",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs && python3 -m unittest discover -s tests -p 'test_*.py'",
    "lint": "node scripts/check-config.mjs"
  },
  "engines": {"node": ">=22.23.2"}
}
